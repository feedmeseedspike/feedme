/**
 * Central configuration for administrative email addresses and settings.
 */

// This is the email that receives order notifications, wallet funding alerts, etc.
// It matches the USER'S new SMTP account.
export const ADMIN_EMAIL = "orders.feedmeafrica@gmail.com"; 

// Optional: Default Sender Email (matches SMTP user)
export const DEFAULT_FROM_EMAIL = "orders.feedmeafrica@gmail.com";
